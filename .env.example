# Vorlage – kopiere nach .env und trage deine Neon-Werte ein.
# Lokal: DIRECT-Connection (ohne "-pooler"). Auf Vercel: POOLED-Connection (mit "-pooler").
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"
